# --- PyTorch (CPU-only version for deployment) ---
# These top three lines are CRITICAL. They force pip to install
# the small, CPU-only version of PyTorch, which is required for deployment.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
torchaudio

# --- Your Application Dependencies ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
PyMuPDF==1.24.1
langchain==0.2.1
tiktoken==0.7.0
sentence-transformers==2.7.0
pinecone-client==4.1.1
numpy==1.26.4
google-generativeai==0.7.1
httpx==0.27.0
